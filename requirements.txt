# Core AgentNet Dependencies
# Based on pyproject.toml and critical missing dependencies identified in audit

# Essential dependencies for basic functionality
typing-extensions>=4.0.0
pyyaml>=6.0

# Critical missing dependencies that block basic functionality
pytest>=7.0.0
pydantic>=2.0.0
prometheus-client>=0.16.0
opentelemetry-api>=1.15.0

# Additional test dependencies
pytest-asyncio>=0.21.0

# Development dependencies for CI/CD
black>=22.0.0
isort>=5.10.0
flake8>=4.0.0
coverage>=6.0.0